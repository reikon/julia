# Public (non-secret) PRODUCTION default for the docs site origin.
# NOTE: Docusaurus does not auto-load .env files. On Vercel, set SITE_URL in the
# project's dashboard env vars (it becomes process.env at build). This file documents
# the intended value. Local dev uses the localhost fallback in docusaurus.config.ts.

SITE_URL=https://peptidia-web.vercel.app
