# Public, non-secret config for the Docusaurus wiki. Copy to .env for committed
# defaults or .env.local for overrides. Not secrets.

# Public origin of the web app (Docusaurus `url`). Used for absolute links and SEO.
# Local dev defaults to http://localhost:3000 if unset (see docusaurus.config.ts).
SITE_URL=
