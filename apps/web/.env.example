# Public, non-secret config for the web (landing) app. Copy to .env.local for
# machine-specific overrides. None of these are secrets.

# This site's own public origin (canonical URLs, OpenGraph). Dev fallback: localhost:3000.
SITE_URL=

# The wiki subdomain the landing page links to. Dev fallback: localhost:3001.
DOCS_URL=
