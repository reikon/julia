{
  "name": "@julia/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Single source of truth for design tokens. Emits token.css (CSS variables) and typed TS exports consumed by both the Next.js/MUI app and the Docusaurus wiki.",
  "exports": {
    ".": "./src/index.ts",
    "./tokens.css": "./tokens.css"
  },
  "scripts": {
    "build": "bun run scripts/build-css.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  }
}
